export enum Status{
    PENDING=0,
    APPROVED=1,
    DISAPPROVED=2
}
export enum AttendanceType{
PRESENT="present",
ABSENT="absent",
HALFDAY="half-day",
FULLDAY="full-day",
PAIDLEAVE="paid-leave"
}
export enum BillingCycle {
  MONTHLY = "monthly",
  YEARLY = "yearly",
}